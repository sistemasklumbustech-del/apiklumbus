node_modules/
dist/
coverage/
*.log

# Secretos -- nunca deben commitearse, solo los .example
.env
.env.*
!.env.example
!.env.*.example

api/uploads/

# Archivos subidos por usuarios (logos, fotos, comprobantes) -- viven en el servidor, no en git
api/uploads/
